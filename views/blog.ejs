<!DOCTYPE html>
<html lang="en">
  <head>
    <%-include('partials/head') %>
    <title>Home</title>
  </head>
  <body>
    <%-include('partials/header') %>
    <div class="container">
      <h1><%=blog.title %></h1>
      <h3>
        Created by
        <img
          src="/uploads/<%=blog.createdBy.profileUrl %>"
          alt="Profile Image"
          class="img-fluid rounded-circle"
          style="object-fit: cover;height: 30px;width: 30px   ;"
          loading="lazy"
        />
        <i><%=blog.createdBy.name %></i>
      </h3>
      <img
        src="/uploads/<%= blog.coverImageUrl %>"
        class="card-img-top m-3"
        alt="Blog Image"
        width="300px"
        height="300px"
        style="object-fit: contain; text-align: center"
        loading="lazy"
      />
      <pre><%=blog.body %> </pre>
    </div>

    <%-include('partials/script') %>
  </body>
</html>
